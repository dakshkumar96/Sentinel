/**
 * Scripted demo scenarios — see PLAN.md demo script.
 */
import type { DemoScenarioId } from "@/types";

export async function runScenario(_id: DemoScenarioId): Promise<void> {
  throw new Error("Not implemented — Phase 1");
}
