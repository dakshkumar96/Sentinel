/**
 * Hero UI — sequential tool activity + agent conclusion + confidence.
 * Phase 4 — stage tool rows with short delays for demo polish.
 */
import type { Investigation } from "@/types";

export function ReasoningTrace(_props: { investigation?: Investigation }) {
  return null;
}
