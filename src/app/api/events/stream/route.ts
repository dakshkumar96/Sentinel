/**
 * SSE endpoint — mock spend + placement events.
 * Phase 1: wire to src/server/mock/stream.ts
 */
export async function GET() {
  return new Response("Not implemented — see PLAN.md Phase 1", { status: 501 });
}
