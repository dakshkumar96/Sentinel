/**
 * POST { scenarioId } — fire scripted demo (judge mode).
 * Phase 1: wire to src/server/mock/scenarios.ts
 */
export async function POST() {
  return Response.json({ error: "Not implemented" }, { status: 501 });
}
