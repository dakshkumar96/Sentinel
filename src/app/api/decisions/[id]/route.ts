/**
 * POST — human Approve / Override / Pause on escalated alert.
 * Phase 4: update alert state + optional mock placement pause
 */
export async function POST() {
  return Response.json({ error: "Not implemented" }, { status: 501 });
}
